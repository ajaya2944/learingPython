#ask user to enter price, if price is >= 500, it is expensive else price is okey
price = int(input("Enter price: "))

if price >= 500:
    print(" Price is Expencive")

else:
    print("Price is Okey")

